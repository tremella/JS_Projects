#!/usr/bin/env node
const runner = require('./runner')
console.log('running test')
// step 1:

// how do we RECURSIVELY find all the .test.js files in a folder? 
// step 2: env setup
// step 3: execution
// step 4: report results